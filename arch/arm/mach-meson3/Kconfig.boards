config MACH_M3_SKT
	depends on ARCH_MESON3=y
	bool "Support M3 (M3_SKT_V1) platform"
	select MESON_IRQ
	default y if ARCH_MESON3
	help
	  Include support for the AMLOGIC M3 Socket Emulation Baseboard platform.

